trước khi làm thì phải

git fetch

sau đó ghi lệnh

git pull origin master

sau đó chỉ cần chạy ở dưới

./run.bat

mac
python app.py

xong

làm xong rồi thì hãy rồi có xác nhận của Minh thì dùng 2 lệnh sau

git fetch

git add .

git commit -m " mô tả những gì đã làm "

git push
